# Zoom API Credentials (Required)
# Get these from https://marketplace.zoom.us/
ZOOM_CLIENT_ID=your_zoom_client_id_here
ZOOM_CLIENT_SECRET=your_zoom_client_secret_here
ZOOM_REDIRECT_URI=http://localhost:8080/redirect
ZOOM_USER_ID=your_zoom_email@example.com

# Optional: Application Configuration
# These can also be set in config.py instead
# Uncomment and modify as needed:

# How many days back to fetch recordings (default: 365)
# RECORDINGS_DATE_RANGE_DAYS=365

# How many last meetings to process (default: 3, set to empty for all)
# LAST_MEETINGS_TO_PROCESS=3

# Folder name template (default: "{date} {time} - {topic}")
# Available placeholders: {date}, {time}, {topic}, {date_time}
# FOLDER_NAME_TEMPLATE="{date} {time} - {topic}"

# Output directory for downloads (default: "test_downloads")
# DOWNLOAD_DIR=test_downloads

# Minimum video length in seconds (default: 60)
# Videos shorter than this will be skipped
# Set to 0 to download all videos regardless of length
# MIN_VIDEO_LENGTH_SECONDS=60
